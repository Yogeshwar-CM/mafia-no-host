# No third-party dependencies. This game runs on the Python standard library
# alone, which is deliberate: it means the server runs anywhere Python 3 does,
# with no install step and nothing to keep patched.
